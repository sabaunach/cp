#include <bits/stdc++.h>

using namespace std;

int main() {
    int v; cin >> v;

    // SA = 2*ab + 2*bc + 2*ca
    // = 2 * (ab + bc + ca)

    // V = a * b * c
    // min(SA) s.t. V = v

    // a=v, b=1, c=1
    // factorize v, all ways to arrange into 3 side lengths?


}
